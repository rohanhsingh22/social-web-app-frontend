# Social Chat Frontend

Next.js frontend for the Social Chat App V1 described in `doc/frontend-chat-app.txt` and `doc/backend-chat-app.txt`.

## Getting Started

```bash
npm install
npm run dev
```

Create `.env.local` from `.env.example` before connecting to the backend.
